# Core LangChain stack
langchain==0.3.25
langchain-community==0.3.25
# >=0.3.65 required by langchain-community 0.3.25 (a clean install rejects 0.3.63)
langchain-core>=0.3.65,<0.4.0
langchain-openai==0.3.19
langchain-text-splitters==0.3.8

# Vector store & PDF ingestion
# langchain-chroma replaces the deprecated langchain_community Chroma import;
# 0.2.x is the line compatible with the pinned langchain-core 0.3.x stack.
langchain-chroma==0.2.4
# BM25 keyword retriever for hybrid (dense + sparse) retrieval
rank-bm25==0.2.2
# NOTE: intentionally unpinned until verified against the deployed build;
# pin to the Space's resolved versions once confirmed.
chromadb
pypdf

# LLM / embeddings
openai==1.109.1
tiktoken==0.12.0

# Web UI: Gradio is installed by the HF Space via README `sdk_version`, so it is
# intentionally NOT pinned here. Gradio 4.44 is NOT viable on HF anymore — the
# HF-injected `spaces` package forces a new FastAPI that breaks 4.44's template
# rendering. app.py only uses `message` (ignores history), so it is compatible
# with modern Gradio 5/6. For local dev, `pip install gradio`.

# Utilities
python-dotenv==1.1.0
numpy
